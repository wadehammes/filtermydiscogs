# Components

How UI is organized under `src/components/` and how we test it.

## Folder layout

- **Feature components**: [`src/components/<Name>/`](../../src/components/) — one folder per component or feature area; **no nested `components/` or `shared/` tiers**. Typically:
  - `Name.component.tsx` — main React export (arrow function, typed props)
  - `Name.module.css` — scoped styles
  - Optional `Name.spec.tsx` + `Name.po.tsx` for tested components (see [conventions.md → Testing](conventions.md#testing))
  - Related pieces are **sibling folders** (e.g. [`ReleaseModal/`](../../src/components/ReleaseModal/) shell + [`ReleaseTracklist/`](../../src/components/ReleaseTracklist/) + [`ReleaseSummaryHero/`](../../src/components/ReleaseSummaryHero/) — not nested under one mega-folder)

- **Shared primitives**: same flat layout under **`src/components/`** — [`AppDialog/`](../../src/components/AppDialog/), [`FormDialog/`](../../src/components/FormDialog/), [`InlinePopoverMenu/`](../../src/components/InlinePopoverMenu/), [`ScrollModal/`](../../src/components/ScrollModal/), [`ModalToolbar/`](../../src/components/ModalToolbar/), [`TanstackChart/`](../../src/components/TanstackChart/), etc. UI icons (SVG + TSX Turbopack fallbacks) live in [`src/styles/icons/`](../../src/styles/icons/) — see [conventions.md → SVG icons](conventions.md#jest-notes). **Base UI** ([`@base-ui/react`](https://base-ui.com/react/overview/quick-start)) powers headless overlays and pickers: [`AppDialog/`](../../src/components/AppDialog/) (controlled **`Dialog`** shell: backdrop + centered popup, focus trap, Escape/backdrop dismiss; **`modal="trap-focus"`** so Base UI does not lock document scroll — overlays call **`usePlaybackPageScrollLock`** from **[`PlaybackPageShell.context.tsx`](../../src/components/PlaybackPageShell/PlaybackPageShell.context.tsx)** to lock the shell scroll root via ref-counted **`lockScroll`/`unlockScroll`**; on mobile when **`data-global-playback-dock`** is set, popup **`top`** / **`bottom`** / **`max-height`** use **`100dvh`** inset tokens (**`--release-modal-viewport-inset-*`**, **`--release-modal-available-height`** in [`playback-dock.module.css`](../../src/styles/modules/playback-dock.module.css)) so modal content stays above the mini player and clear of mobile browser chrome), [`InlinePopoverMenu/`](../../src/components/InlinePopoverMenu/) (shared **`Menu`** popup shell — see [conventions.md → Inline popover menus](conventions.md#css-and-styling)), [`ScrollModal/`](../../src/components/ScrollModal/) (scrollable card modal built on **`AppDialog`**), plus feature components on **`Select`**, **`AutocompleteSelect`**, **`FilterMatchOperatorSelect`**, and **`Menu`** — filter controls ([`Select/`](../../src/components/Select/), [`AutocompleteSelect/`](../../src/components/AutocompleteSelect/), [`FilterMatchOperatorSelect/`](../../src/components/FilterMatchOperatorSelect/)), header/crate menus ([`UserActions`](../../src/components/StickyHeaderBar/UserActions.tsx), [`CrateDetailActionsMenu`](../../src/components/Crates/CrateDetailActionsMenu.component.tsx)). Global portal setup: [`.appRoot`](../../src/app/layout.tsx) + [`base-ui-setup.css`](../../src/styles/global/base-ui-setup.css) (`isolation: isolate`; `body { position: relative }` for iOS Safari backdrops). Shared backdrop tokens: [`base-ui-portal.module.css`](../../src/styles/modules/base-ui-portal.module.css) (composes **`enterexitfade`**). Modal/dialog enter-exit: [`portal-enter-exit.module.css`](../../src/styles/modules/portal-enter-exit.module.css) on **`AppDialog`** popup classes (**`enterexitscale`**). Dashboard charts: [`TanstackChart/`](../../src/components/TanstackChart/) (motion renderer host + shared axis/grid/tooltip styling), [`GrowthAreaChart/`](../../src/components/GrowthAreaChart/) (admin growth areas), [`PieChartLegend/`](../../src/components/PieChartLegend/) (HTML percent labels under donut pies). [`ModalToolbar/`](../../src/components/ModalToolbar/) (microdot toolbar: optional **`children`** lead actions via **`ModalToolbarLink`** or custom controls such as **`ReleaseCrateMenu`** composing **`.actionButton`** / **`.actionButtonActive`**, optional **`title`**, close **X**).

- **Providers**: [`src/components/Providers.tsx`](../../src/components/Providers.tsx) — root QueryClient + context stack. Global toasts use **Base UI** [`Toast`](https://base-ui.com/react/components/toast) via [`AppToaster`](../../src/components/AppToaster/AppToaster.component.tsx) (dual viewports: bottom-right default, bottom-center for action/loading toasts; **`--z-8-toast`** above modals/tooltips). Type-based styling and layout live in [`AppToaster.module.css`](../../src/components/AppToaster/AppToaster.module.css) (**`--font-family-mono`**, **`--background`**-based surfaces, semantic tints for success/error; success toasts include a checkmark icon). Import **`toast`** from [`src/utils/toast.ts`](../../src/utils/toast.ts) for user feedback — prefer toasts over **`alert()`** for non-blocking errors and success messages. Auth session feedback on `/` uses [`AuthCheckingToast`](../../src/components/AuthCheckingToast/AuthCheckingToast.component.tsx). After the **3rd** completed OAuth login (server **`User.login_count`**), [`SupportProjectToast`](../../src/components/SupportProjectToast/SupportProjectToast.component.tsx) may show a bottom-center action toast when **`GET /api/auth/check`** returns **`showSupportProjectToast: true`**; dismissal persists via **`useDismissSupportProjectToastMutation`** → **`POST /api/user/support-toast/dismiss`**. Analytics consent uses [`CookieConsentBanner`](../../src/components/CookieConsentBanner/CookieConsentBanner.component.tsx) inside **`AnalyticsShell`** (theme tokens + fixed-bottom chrome aligned with **`ReleaseMiniPlayer`**).
- **Public pages**: [`PublicAuthLayout`](../../src/components/PublicAuthLayout/PublicAuthLayout.component.tsx) is the single client shell for home, about, legal, and public crate pages (`data-testid="fmdPublicAuthLayout"`). Server `page.tsx` files pass [`PageFooter`](../../src/components/Page/PageFooter.server.tsx) as the `footer` prop. [`PublicAuthHeader`](../../src/components/PublicAuthLayout/PublicAuthHeader.component.tsx) renders [`PublicPageHeader`](../../src/components/PublicPageHeader/PublicPageHeader.component.tsx) for visitors or [`StickyHeaderBar`](../../src/components/StickyHeaderBar/StickyHeaderBar.component.tsx) when `authenticatedNavPage` is set and the user is signed in. Props: `currentPage` (`home` \| `about` \| `legal`), `centerMain` (home), `authenticatedNavPage`, optional `header` override, optional `footer`. **[`AboutClient`](../../src/components/About/AboutClient.component.tsx)** (`data-testid="fmdAbout"`) uses a **bento grid** ([`About.module.css`](../../src/components/About/About.module.css)): intro tile, compact feature tiles with [`LoginFeatureVisual`](../../src/components/Login/LoginFeatureVisual.component.tsx), [`AboutDonationSection`](../../src/components/About/AboutDonationSection.component.tsx) (`#support`), GitHub/contact tiles ([`about.constants.ts`](../../src/constants/about.constants.ts)), and a **Clear stored data** tile ([`useConfirmClearAllUserData`](../../src/hooks/useConfirmClearAllUserData.hook.ts)). Stripe checkout via **`POST /api/donate/checkout`** (**$5**, **$10**, **$25** presets, or custom USD **$1**–**$10,000**). Legal copy: [`LegalPageContent.server.tsx`](../../src/components/Legal/LegalPageContent.server.tsx). Public crate view: [`PublicCrateClient.component.tsx`](../../src/components/PublicCrate/PublicCrateClient.component.tsx).
- **Landing page**: [`Login.component.tsx`](../../src/components/Login/Login.component.tsx) (`data-testid="fmdLogin"`) is the home page body: intro heading (**`SITE_NAME`** in **`.visuallyHidden`**, visible **`SITE_TAGLINE`** / **`SITE_LEAD`**), [`LoginPreviewDemo`](../../src/components/Login/LoginPreviewDemo.component.tsx), OAuth hero + bottom CTAs ([`LoginConnectButton`](../../src/components/LoginConnectButton/LoginConnectButton.component.tsx), [`LoginBottomCta`](../../src/components/LoginBottomCta/LoginBottomCta.component.tsx)), a **What you can do** features intro (**`LOGIN_PAGE_UI_COPY.featuresSectionHeading`** / **`featuresSectionLede`**), and feature rows ([`LoginFeatureRow`](../../src/components/LoginFeatureRow/LoginFeatureRow.component.tsx) + [`loginFeatures.constants.ts`](../../src/components/Login/loginFeatures.constants.ts)). Marketing strings are centralized in [`loginPageCopy.registry.ts`](../../src/constants/loginPageCopy.registry.ts) and validated by [`loginPageCopyLiteraryRules.spec.ts`](../../src/tests/utils/loginPageCopyLiteraryRules.spec.ts) (no em dashes, embellishment, or inaccurate claims). The crates row highlights the owner detail page: reordering, set markers, set notes, gig packing, and public sharing. Styles: [`Login.module.css`](../../src/components/Login/Login.module.css). Theme screenshots: [`LoginFeatureVisual`](../../src/components/Login/LoginFeatureVisual.component.tsx). Home JSON-LD: [`HomeJsonLd`](../../src/components/Login/HomeJsonLd.component.tsx) on [`page.tsx`](../../src/app/page.tsx). [`LoginConnectButton`](../../src/components/LoginConnectButton/LoginConnectButton.component.tsx) shows the Discogs SVG with **“Connect with Discogs”** as the accessible name (visible “Discogs” text is `.visuallyHidden` from [`accessibility.module.css`](../../src/styles/modules/accessibility.module.css)). Use it for **every OAuth connect CTA** (landing hero/bottom, public crate “Get Started”, etc.); wire `onClick` to [`useAuth().login`](../../src/context/auth.context.tsx). Public nav **“Log in”** in [`PublicPageHeader`](../../src/components/PublicPageHeader/PublicPageHeader.component.tsx) is a plain **`<a href="/api/auth/discogs">`** (full document navigation — not **`next/link`**) so token reuse and OAuth redirects apply cookies correctly. Visitors use **`system`** theme (OS light/dark); theme selection is in **Settings** after login.

## Naming

- Prefer **`.component.tsx`** for the main React export in a feature folder.
- **Client page shells** use **`*Client.tsx`** or **`*Client.component.tsx`** under [`src/components/`](../../src/components/), imported from a server [`page.tsx`](../../src/app/about/page.tsx) in **`src/app/`** (routing only — no co-located components or CSS modules in route folders).
- Keep **one primary component file** per folder unless the feature is large enough to justify split files (hooks, subcomponents).

## CSS Modules

Import as `import styles from "./Name.module.css"` and reference **`styles.className`**. Use **`classNames`** whenever classes are combined or conditional — static lists, object notation for state, and optional `className` props (see [conventions.md → React / JSX](conventions.md#react--jsx)). Do **not** cast module keys with **`as string`** in object notation. Single unconditional module classes may use `className={styles.block}` directly.

## Scaffolding

Run **`pnpm scaffold <ComponentName>`** ([`scripts/scaffold.sh`](../../scripts/scaffold.sh)) to generate:

- `Name.component.tsx` with root **`data-testid="fmd<Name>"`**
- `Name.module.css`
- `Name.spec.tsx` (assertions via **`screen`**; imports PO first)
- `Name.po.tsx` with typed **`renderName(overrides)`**, defaults + `{...overrides}`, and optional private **`*Element()`** helper (see [conventions.md → Testing](conventions.md#testing))

Add factories under **`src/tests/factories/`** when structured test data is needed.

## Links and media

- Internal routes: **`next/link`** or **`useRouter`** from `next/navigation`.
- Discogs release/artist/label URLs: **`getResourceUrl({ resourceUrl, type, id? })`** from [`src/utils/helpers.ts`](../../src/utils/helpers.ts) — parses **`resource_url`**, or falls back to a numeric **`id`** when the collection payload omits URLs.
- Cover art: **`next/image`** with Discogs CDN URLs (`i.discogs.com`).

## Test IDs

- Component root: **`data-testid="fmd<ComponentName>"`** (PascalCase component name).
- Page object **`testId`** must match exactly.
- Specs query with **`screen.getByTestId(po.testId)`** after PO render setup.

## Testing

- Colocate specs with the component when adding coverage (`*.spec.tsx`; optional **`*.po.tsx`**).
- Use **`render`** from **`test-utils`** when auth, filters, or crate context is required **without** mocking (default wrapper is **`TestProviders`**). Context-heavy component tests usually mock context in the PO and use **`render`** instead (see [conventions.md → Testing](conventions.md#testing)).
- **Page composition in POs**: When production mounts a component inside a layout plus server footer (e.g. home = `PublicAuthLayout` + `PageFooter` + `Login`), the PO **`render*`** helper should mirror that tree. Put **`jest.mock("src/components/Page/PageFooter.server")`** in the PO and render `<PageFooter />` in the layout’s `footer` prop—do not inline a partial footer that omits links the spec asserts on.

## Exports

Import from the **concrete module path** (e.g. `src/components/ReleaseCard/ReleaseCard.component.tsx`). Avoid new barrel **`index.ts`** files.

## Feature example: ReleaseCard (desktop grid)

[`ReleaseCard`](../../src/components/ReleaseCard/ReleaseCard.component.tsx) and **`PublicReleaseCard`** share [`ReleaseCard.module.css`](../../src/components/ReleaseCard/ReleaseCard.module.css). Body copy lives in [`ReleaseCardContent`](../../src/components/ReleaseCard/ReleaseCardContent.component.tsx).

| Concern | Pattern |
|---------|---------|
| Shell | Inset **`1px`** stroke via **`box-shadow`** on **`--border`** (no physical **`border`** — keeps cover art flush with rounded corners); **`--shadow-sm`** at rest; on **`@media (hover: hover)`**, hover swaps inset stroke to **`--input`**, lifts with **`--shadow-lg`** + **`--background`**-tinted outer shadow, and **`translateY(-2px)`** |
| Cover | **`.imageContainer`** has no corner radius — **`.releaseCard`** **`overflow: hidden`** clips the blurred background; centered thumb (**65%**) keeps its own **`--radius-card`**. Blurred cover backdrop via **`::before`** + **`--release-cover-image`** (hover-capable pointers only) |
| Title | [`ReleaseCardTitle`](../../src/components/ReleaseCard/ReleaseCardTitle.component.tsx) composes shared [`ReleaseHeaderLinks`](../../src/components/ReleaseCard/ReleaseHeaderLinks.component.tsx) (**`ReleaseHeaderArtistLine`**, **`ReleaseHeaderTitle`**) + [`ReleaseCardMeta`](../../src/components/ReleaseCard/ReleaseCardMeta.module.css) — **single-line** ellipsis (artist, title, label/year); **`headerTextStack`** / **`.titleGroup`** / mobile **`.releaseInfo`** use **`gap: var(--space-1)`** to match modal hero **`ReleaseSummaryHero`** **`.detailsText`**; card artist/label use **`--text-meta-sm`** / **`--text-meta-xs`**; title stays bold sans at **`clamp(… cqi …)`** |

| Highlight | **`.highlighted`** — **`2px`** primary inset stroke in **`box-shadow`** (plus highlight glow shadows; composes **`theme-highlighted-surface`**) |
| In crate | **`.inCrate`** — primary **`outline`** on the card shell when the release is in the **active** (currently viewed) crate only — **`ReleasesGrid`** passes **`inActiveCrate`** from **`useCrateState().activeCrateInstanceIds`** so memoized cards avoid subscribing to full **`useCrate()`**; membership in other crates is shown only in **`ReleaseCrateMenu`** checkboxes |
| Notes | Cover overlay **`ReleaseNotesCardAction`** only — no inline note body on the card |

## Releases grid ([`ReleasesGrid`](../../src/components/ReleasesClient/ReleasesGrid.component.tsx))

Card view renders via [`ReleasesGrid.module.css`](../../src/components/ReleasesClient/ReleasesGrid.module.css) (not the duplicate **`.releasesGrid`** block in [`ReleasesClient.module.css`](../../src/components/ReleasesClient/ReleasesClient.module.css) — keep both in sync if either changes).

| Concern | Pattern |
|---------|---------|
| Columns | From **`768px+`**: **`repeat(auto-fit, minmax(280px, 1fr))`** — use **`auto-fit`**, not **`auto-fill`**, so visible cards stretch across the full main column when there are fewer items than would fit (e.g. filtered list or crate drawer closed on desktop). **`auto-fill`** reserves empty tracks and leaves a dead gap on the right. **`.releaseItem`** caps card width at **`20rem`** on desktop so sparse filters do not produce viewport-sized tiles |
| Workspace | [`ReleasesClient`](../../src/components/ReleasesClient/ReleasesClient.component.tsx): **`data-releases-workspace`**, **`.withSidebar`** when the crate drawer is open; desktop **`.sidebar`** (**`440px`**) only mounts when open — **`mainContent`** **`min-width: 0`** so the grid can shrink beside the drawer |
| Infinite scroll | [`useReleasesClient`](../../src/hooks/useReleasesClient.hook.ts) windowed render: first **100** cards, **`+100`** when the [`LoadingTrigger`](../../src/components/ReleasesClient/LoadingTrigger.component.tsx) sentinel enters view (**`react-intersection-observer`**, **`root`** = scroll container, **`threshold: 0`**, **`rootMargin: 0px 0px 750px 0px`** — prefetch when the sentinel is within **750px** of the scroll bottom). **`visibleCount`** resets to **100** only when filter/sort/search deps change — **not** when the collection grows during pagination. Card view is a normal CSS grid (no virtualizer). List view virtualizes in [`ReleasesTable`](../../src/components/ReleasesTable/ReleasesTable.component.tsx) (fixed estimates; **`useRegisterPlaybackPageScrollToTop`** for **`BackToTop`**). [`ReleasesClient`](../../src/components/ReleasesClient/ReleasesClient.component.tsx) passes **`scrollElement`** from **`[data-releases-scroll-root]`** into the hook because **`ReleasesClientContent`** runs above **`PlaybackPageShellProvider`** — context **`usePlaybackPageScrollElement()`** alone is **`null`** there — and into **`CollectionPlaybackPageShell`** for the provider |
| Loading sentinel | [`LoadingTrigger`](../../src/components/ReleasesClient/LoadingTrigger.component.tsx) — **`data-testid="fmdReleasesLoadingTrigger"`**; **`infiniteScrollRef`** for the sentinel; spinner overlay while **`isFetchingNextPage`**. Hook specs trigger intersection via [`setupIntersectionObserverMock`](../../src/tests/mocks/mockIntersectionObserver.mock.ts); component specs use the same mock + [`ReleasesClient.po.tsx`](../../src/components/ReleasesClient/ReleasesClient.po.tsx) **`mockCollectionWithReleaseCount(n)`** |
| Active crate ring | **`ReleasesGrid`** reads **`activeCrateInstanceIds`** once and passes **`inActiveCrate`** into memoized **`ReleaseCard`** / **`MobileReleaseCard`** — cards do not subscribe to crate context for the ring alone |

## Feature example: MobileReleaseCard

[`src/components/ReleaseCard/MobileReleaseCard.component.tsx`](../../src/components/ReleaseCard/MobileReleaseCard.component.tsx) is the **mobile** release row (image | content | action column). **`PublicMobileReleaseCard`** shares **`MobileReleaseCard.module.css`**.

| Concern | Pattern |
|---------|---------|
| Shell | Inset **`1px`** stroke via **`box-shadow`** on **`--border`** (no physical **`border`**); **`--shadow-sm`** at rest; on **`@media (hover: hover)`**, hover swaps inset stroke to **`--input`**, adds **`--shadow-md`** + **`--background`**-tinted outer shadow |
| Layout | Horizontal flex: fixed **`7rem`** cover, **`flex: 1 1 0`** content (**`min-width: 0`**), **`4rem`** action column with full-height overlay actions. **Random view** on mobile uses desktop [`ReleaseCard`](../../src/components/ReleaseCard/ReleaseCard.component.tsx) (vertical showcase) via [`ReleasesGrid`](../../src/components/ReleasesClient/ReleasesGrid.component.tsx)—not this row layout |
| Cover | **`.imageContainer`** has no corner radius — card **`overflow: hidden`** clips the cover column; inner **`img`** / **`releaseImage`** keep left-edge radius via **`calc(var(--radius-card) - var(--release-card-border-width))`** |
| Loading | [`MobileReleaseCardSkeleton`](../../src/components/ReleasesClient/MobileReleaseCardSkeleton.component.tsx) mirrors this row layout while collection pages stream in; desktop uses [`DesktopReleaseCardSkeleton`](../../src/components/ReleasesClient/DesktopReleaseCardSkeleton.component.tsx) |
| Pills | **`HorizontalScrollRow`** — wrapper needs **`min-width: 0`** + **`overflow: hidden`** so pill rows scroll horizontally instead of wrapping (same component in [`ReleasesTable`](../../src/components/ReleasesTable/ReleasesTable.component.tsx)); wheel/trackpad only hijacks dominant horizontal deltas ( **`Shift`+wheel** included) and releases vertical scroll to the page at horizontal scroll ends |
| Title block | Artist + title + meta grouped in **`.releaseInfo`** with **`titleGroupMobile`** / **`metaLineMobile`** / **`catalogRowMobile`** — **`gap: var(--space-1)`** between rows (same rhythm as modal hero); title stays **single-line** ellipsis (same as desktop); pills keep looser outer gaps |
| Highlight | **`.highlighted`** — **`2px`** primary border + **`theme-highlighted-surface`** |
| In crate | **`.inCrate::after`** draws a full-card primary ring on top of artwork when the release is in the **active** crate only (same **`inActiveCrate`** rule as desktop) |
| Notes | No action-column notes control — edit in **`ReleaseModal`** only |
| Open detail modal | Cover art or **Release details** overlay button call optional **`onReleaseClick`**; title links to Discogs in a new tab |
| Crate menu | **`ReleaseCrateMenu`** popover (last action column): **`min-height: var(--touch-target-min)`** rows below **`1024px`**; desktop trigger uses **`aria-label`** plus a hover-only custom tooltip (no native **`title`**) — see [conventions.md → CSS and styling](conventions.md#css-and-styling) (**Segmented controls**, **Inline popover menus**) |
| Overlay actions | **`ReleaseCardOverlayActions`**: desktop cover overlay uses a segmented group (release details **`VinylThinIcon`**, **Add to queue** **`ListPlusThinIcon`** — with a primary **dot** badge (**`fmdReleaseQueueIndicator`**) when every playable track is already queued — or a **`Spinner`** while release detail is fetched — via **`useReleaseCardQueueAction`** (fetches release detail on click when needed, adds all embeddable album tracks not already queued with success/error toasts from **`releaseCardQueueToast`**, same matching rules as the modal **Add all to queue** toolbar), notes, View on Discogs) plus a **standalone crate menu** (**`ReleaseCrateMenu`**) to its right (same row height as the segmented group): Base UI **`Menu.CheckboxItem`** rows — **current crate** pinned above a scroll list (max four other rows) with a dashed divider and **`useScrollEdgeFade`** top/bottom fades; membership prefetched on trigger hover/focus/pointer-down via **`prefetchReleaseCrateMembership`**, subscribed when the menu opens via **`useReleaseCrateMembershipQuery`**, toggles call **`addReleaseToCrate`** / **`removeReleaseFromCrate`** (menu stays open); footer **Add to all** / **Remove from all** (when two or more crates) and **Add to new crate** (**`PlusIcon`**) each in their own composed **`footer`** block so **`border-top`** separators divide crate list, bulk toggle, and create action; **Add to new crate** opens **`CreateCrateDialog`** (name + optional **Set as default crate**, then **`createCrate`**, optional **`updateCrate` `{ is_default: true }`**, and **`addReleaseToCrate`**) menu trigger active styling matches the card **`.inCrate`** rule (**active** crate only — **`ReleasesGrid`** passes **`inActiveCrate`** from **`useCrateState().activeCrateInstanceIds`** so memoized cards avoid full **`useCrate()`** subscriptions); adding to the **current** crate still opens the drawer on desktop; mobile keeps a single full-height column (details → queue → Discogs → crate menu last) with **`border-top`** separators — same list-plus icon and dot badge when queued (shared dot chrome from [`action-indicator-dot.module.css`](../../src/styles/modules/action-indicator-dot.module.css)) |

## Feature example: ReleaseCrateMenu

[`ReleaseCrateMenu`](../../src/components/ReleaseCard/ReleaseCrateMenu.component.tsx) is the shared multi-crate add/remove popover. Callers pass **`actionClass`** (and optional **`slotClass`**) so the trigger matches local chrome; optional **`triggerVariant`** (**`"card"`** default — card overlay button styles; **`"custom"`** — caller classes only) and **`triggerStyle`** (**`"icon"`** default; **`"text"`** — **"+ Add to Crate"** / **"− Remove from Crate"** label on the trigger, e.g. [`ReleaseListItem`](../../src/components/ReleaseListItem/ReleaseListItem.component.tsx)). Trigger sets **`aria-pressed`** when the release is in the **active** crate. Portaled **`Menu.Positioner`** uses **`usePortaledOverlayContainer`** so the popup mounts in the nearest **`OverlayStack`** (e.g. **`AppDialog`** / **`ScrollModal`** — see [conventions.md → Inline popover menus](conventions.md#css-and-styling)).

| Mount point | Trigger pattern |
|-------------|-----------------|
| [`ReleaseCardOverlayActions`](../../src/components/ReleaseCard/ReleaseCardOverlayActions.component.tsx) | Default **`triggerVariant="card"`**; **`layout="vertical"`** on mobile card column |
| [`ReleaseListItem`](../../src/components/ReleaseListItem/ReleaseListItem.component.tsx) | **`triggerVariant="custom"`** + **`triggerStyle="text"`**; row **`.inCrate`** from **`useCrateState().activeCrateInstanceIds`** |
| [`ReleasesTable`](../../src/components/ReleasesTable/ReleasesTable.component.tsx) | First column — compact icon trigger (**`triggerVariant="custom"`**, **`.crateTrigger`**) |
| [`ReleaseMiniPlayer`](../../src/components/ReleaseMiniPlayer/ReleaseMiniPlayer.component.tsx) | Circular transport-bar control (**`triggerVariant="custom"`**, **`.controlButton`**) |
| [`ReleaseSummaryHeroToolbar`](../../src/components/ReleaseSummaryHeroToolbar/ReleaseSummaryHeroToolbar.component.tsx) / [`ReleaseSummaryHero`](../../src/components/ReleaseSummaryHero/ReleaseSummaryHero.component.tsx) | **`ModalToolbar`** **`.actionButton`** / **`.actionButtonActive`** |
| [`ReleaseSimilarReleaseItem`](../../src/components/ReleaseSimilarReleaseItem/ReleaseSimilarReleaseItem.component.tsx) | Vertical **`overlayAction`** stack (modal similar sidebar) |

Do **not** call legacy **`addToCrate`** / **`removeFromCrate`** toggles on these surfaces — use **`ReleaseCrateMenu`** (or **`useCrateActions`** from the menu’s checkbox handlers) so membership is per-crate. **`addReleaseToCrate`** for the **current** crate still opens the drawer on desktop when appropriate.

## Feature example: CrateDrawerReleaseItem

[`CrateDrawerReleaseItem`](../../src/components/CrateDrawerReleaseItem/CrateDrawerReleaseItem.component.tsx) + [`CrateDrawerReleaseActions`](../../src/components/CrateDrawerReleaseActions/CrateDrawerReleaseActions.component.tsx) render each release row in the **`/releases`** drawer and share the same horizontal layout as **`MobileReleaseCard`** (cover | meta | **`4rem`** action column). Styles live in [`CrateDrawerReleaseItem.module.css`](../../src/components/CrateDrawerReleaseItem/CrateDrawerReleaseItem.module.css); actions compose [`vertical-action-stack.module.css`](../../src/styles/modules/vertical-action-stack.module.css).

| Concern | Pattern |
|---------|---------|
| Shell | **`--card`** background; **`1px`** border on **`--border`**; **`--shadow-sm`** at rest; on **`@media (hover: hover)`**, hover lightens the border via **`color-mix(..., var(--foreground))`** and adds a foreground-tinted outer shadow — do **not** swap the row to **`--muted`** (avoids a harsh cover vs. text split on warm palettes) |
| Layout | Clickable main area (cover + artist/title/meta) opens **`ReleaseModal`**; action column is a full-height stack separated by **`border-left`** |
| Packed | **`.listItemFound`** — primary outer border when **`packed_enabled`** and release is marked packed; packed toggle sets **`aria-pressed`** |
| Action hover | Default stack buttons tint with **`color-mix(..., var(--card))`**; packed toggle keeps primary fill on hover; remove uses a destructive tint on **`--card`**, not **`--background`** |

## Feature example: ReleasesTable

[`ReleasesTable`](../../src/components/ReleasesTable/ReleasesTable.component.tsx) is the desktop **list** view on **`/releases`** when view mode is list (see [architecture.md](architecture.md) → view atoms). TanStack Table renders crate menu, cover, artist/title, label, year, **Format/Styles**, and notes columns.

| Concern | Pattern |
|---------|---------|
| Crate column | **`ReleaseCrateMenu`** icon trigger (**`triggerVariant="custom"`**) in the first column — same multi-crate popover as release cards; row **`.inCrate`** highlights when the release is in the **active** crate only (**`useCrateState().activeCrateInstanceIds`**) |
| Format/Styles column | Format pills plus genre/style tags from [`getReleaseGenreStyleTags`](../../src/utils/releaseGenreStyleTags.ts), wrapped in **`HorizontalScrollRow`** inside a fixed-width cell (**`min-width: 0`**) so tags scroll horizontally instead of wrapping |
| Column resizing | TanStack Table v9 **`columnResizingFeature`**: drag header edges to resize (double-click resets); widths persisted in **`localStorage`** via **`RELEASES_TABLE_LAYOUT_STORAGE_KEY`** ([`useReleasesTableLayout.hook.ts`](../../src/components/ReleasesTable/useReleasesTableLayout.hook.ts)). Each resizable header uses a **`<button type="button">`** handle (**`aria-label`**, wide hit area, 3px **`::after`** rail) — not a static **`<div>`** — so Biome **`noStaticElementInteractions`** passes. Wide tables scroll inside **`.tableContainer`** — **`min-width: 0`** on **`.mainContent`** (releases grid) and table wrappers keeps the crate sidebar in the layout |
| Filter pills | Same **`usePillClickHandler`** / **`useFilterAtoms`** wiring as release cards — pills toggle format and genre/style filters |
| Notes | **`ReleaseNotes variant="table"`** — no per-row **`ReleaseNotesEditorProvider`** (see ReleaseNotes feature example) |

## Feature example: ReleaseModal

[`ReleaseModal/`](../../src/components/ReleaseModal/) is the release detail dialog shell on **`/releases`**, **`/dashboard`**, and owner **`/crates/[id]`**. Body, hero, tracklist, similar sidebar, and public variants live in sibling folders (see table). Opened when the user clicks cover art or title on a collection card (via **`onReleaseClick`** from [`useSelectedReleaseModal`](../../src/hooks/useSelectedReleaseModal.hook.ts) — also used by [`useReleasesClient`](../../src/hooks/useReleasesClient.hook.ts) on **`/releases`**).

**URL routing:** Modal open state syncs to **`?instance=<instance_id>`** ([**`RELEASE_MODAL_INSTANCE_PARAM`**](../../src/constants.ts)) on the current page. Every open and similar-sidebar switch **`router.push`**es (preserving other query params) so browser **Back** walks through viewed releases; explicit close (**X**, Escape, backdrop) **`router.replace`**s to the URL captured before the first open in that session; landing on a direct link with **`?instance=`** closes via **`router.replace`** to strip the param. **`useSelectedReleaseModal`** drives optimistic open/close through a **`modalSyncReducer`** so UI does not wait on App Router **`searchParams`** updates; **`prefetchReleaseModal`** / [`releaseModalLoader.ts`](../../src/components/ReleaseModal/releaseModalLoader.ts) warm the lazy modal chunk on first click. Unknown instance ids leave the modal closed. Helpers: [`releaseModalUrl.ts`](../../src/utils/releaseModalUrl.ts). Host pages that call **`useSearchParams`** wrap the client in **`<Suspense>`** ([**`/releases`**](../../src/app/releases/page.tsx), [**`/dashboard`**](../../src/app/dashboard/page.tsx); crate routes already do).

**Collection data:** Personal rating, notes, and hero metadata resolve from the **Discogs collection React Query cache**, not a separate modal copy. [`useSelectedReleaseModal`](../../src/hooks/useSelectedReleaseModal.hook.ts) takes **`collectionUsername`** on **`/releases`** and **`/dashboard`**, resolves the open release from a memoized **`fallbackReleases`** index first (visible/filtered list for instant open), then [`useCollectionReleaseByInstanceId`](../../src/hooks/queries/useCollectionReleaseByInstanceId.hook.ts) — which prefers **`allReleasesAtom`** over rebuilding from query pages via [`buildReleaseIndexFromList`](../../src/utils/collectionReleaseLookup.ts). Owner **`/crates/[id]`** and public **`/crate/[id]`** pass crate-scoped **`fallbackReleases`**. Tracklist and community rating still come from **`useDiscogsReleaseQuery`**. Rating/notes writes patch the infinite collection cache + IndexedDB ([`collectionCacheSync.ts`](../../src/utils/collectionCacheSync.ts), helpers in [`collectionReleaseLookup.ts`](../../src/utils/collectionReleaseLookup.ts)); [`useCollectionData`](../../src/hooks/useCollectionData.hook.ts) keeps **`allReleasesAtom`** in sync for filters and the grid.

| File | Role |
|------|------|
| `ReleaseModal.component.tsx` | Backdrop + scroll shell via **`ScrollModal`**, hero header (cover, metadata, crate/Discogs/close), scrollable body, analytics on open. When similar matches exist or are loading, applies **`modalWide`** on the dialog popup (**`--scroll-modal-max-width: 68rem`** at **`1024px+`** via CSS). Desktop (≥1024px, **`useMediaQuery`**) also applies **`modalMain`** on the inner scroll content (**`splitContent`**) and mounts **`ReleaseSimilarSidebar`** in **`aside`** with **`ReleaseSummaryHeroToolbar`** in **`toolbar`** (hero omits its toolbar); mobile keeps the standard shell and passes similar matches inline in the body |
| `ReleaseModalBody.component.tsx` | Tracklist via **`useReleaseModalPlayback`**, notes, then below **`1024px`** an inline **`ReleaseSimilarSidebar`** card below notes. Inner **`modalCard`** sections use bordered cards with horizontal inset on all breakpoints. While release detail or collection fields load, **`ReleaseTracklistSkeleton`** and **`ReleaseNotesModalSkeleton`** mirror the tracklist and modal notes layout |
| `ReleaseSimilarSidebar.component.tsx` | **`variant="aside"`** (desktop **`ScrollModal`** column — independently scrollable list) or **`variant="inline"`** (mobile card in main scroll). Shared genres/styles via **`getSimilarReleases`** ([`similarReleases.ts`](../../src/utils/similarReleases.ts)) |
| `ReleaseSimilarReleaseItem.component.tsx` | Crate-drawer-style row (**`CrateDrawerReleaseItem`** layout + compact overrides in **`ReleaseSimilarReleaseItem.module.css`**) with **`ReleaseCrateMenu`** vertical stack action; **`.listItemInCrate`** primary border when staged in the **active** crate; row click calls **`onReleaseClick`** |
| `ReleaseSummaryHeroToolbar.component.tsx` | Crate/Discogs/close toolbar extracted for the desktop split layout when similar matches exist; crate control is **`ReleaseCrateMenu`** with **`ModalToolbar`** **`.actionButton`** / **`.actionButtonActive`** styling (**`aria-pressed`** when in the active crate) |
| `ReleaseHeaderLinks.component.tsx` | Shared artist/title Discogs links for cards and modal heroes — **`ReleaseHeaderArtistLine`** (**`<div>`**, not **`<p>`**) and **`ReleaseHeaderTitle`** (**`h2`** in heroes, **`h3`** on cards); links call **`getResourceUrl`** (with **`id`** fallback) and **`stopPropagation`** so modal dismiss handlers do not swallow clicks; title line-clamp lives on **`.heroTitleLink`** in [`ReleaseSummaryHero.module.css`](../../src/components/ReleaseSummaryHero/ReleaseSummaryHero.module.css) |
| `useSimilarReleasesInCollection.hook.ts` | Memoized similar-release list from **`allReleasesAtom`** (limit **`RELEASE_MODAL_SIMILAR_LIMIT`**); exposes **`isSimilarLoading`** while collection pagination is in flight |
| `ReleaseSummaryHero.component.tsx` | Shared **`ModalToolbar`** (crate, Discogs, close **X**) + stacked cover/metadata on mobile, side-by-side on desktop; artist and title link to Discogs via **`ReleaseHeaderLinks`**; enriches artist/label ids from **`useDiscogsReleaseQuery`** via [`mergeReleaseHeaderArtists`](../../src/utils/releaseHeaderMetadata.ts) / [`mergeReleaseHeaderLabel`](../../src/utils/releaseHeaderMetadata.ts); catalog meta via **`ReleaseCardMeta`** with **`metaClassName`** composing **`metaCaption`** (clickable label, year, catno); artist line also composes **`metaCaption`**; title **`--text-lg`**; then **`ReleaseHeroRatingsRow`** (personal stars + community average), then clickable format pills plus genre/style tags from **`getReleaseGenreStyleTags`** |
| `ReleaseHeroRatingsRow.component.tsx` | One ratings row — **`ReleasePersonalRating`** (authenticated) + community average, `·`-separated when both present |
| `ReleasePersonalRating.component.tsx` | Auth-only wrapper mounting **`useReleaseRatingEditor`** + **`ReleaseRatingPicker`** |
| `ReleaseRatingPicker.component.tsx` | Native **`<input type="radio">`** stars in a **`<fieldset>`**; hover preview highlights included stars and dims higher stars |
| `useReleaseRatingEditor.hook.ts` | Rating write handler; patches **`DiscogsCollectionQueryKeys`** + IndexedDB (modal reads collection cache via **`useCollectionReleaseByInstanceId`**) without refetching the collection |
| `PublicReleaseModal.component.tsx` | Public crate variant — **`ScrollModal`** shell, **`PublicReleaseSummaryHero`** + **`PublicReleaseModalBody`** (tracklist only) |
| `PublicReleaseSummaryHero.component.tsx` | Discogs + close toolbar; same **`ReleaseHeaderLinks`** + **`ReleaseCardMeta`** **`metaCaption`** pattern as **`ReleaseSummaryHero`** (clickable artist, title, label, year, catno); **`ReleaseHeroRatingsRow`** (community average only); static format pills plus genre/style tags from **`getReleaseGenreStyleTags`** (no filter actions, no crate toggle) |
| `PublicReleaseModalBody.component.tsx` | Tracklist/playback only — no notes section |
| `ReleasePlaybackFallback.component.tsx` | YouTube search + external video links when no embeddable video is available |
| `ReleasePlaybackPreview.component.tsx` | Below the tracklist: **`ReleaseTracklist`** with **`hideTrackPosition`** for embeddable videos not matched to any track (**`getReleasePreviewVideos`**) — title-only left column (no position), duration, add to queue, row click to play |
| `ReleaseTracklist.component.tsx` | Track rows are clickable only when **`isTrackPlayable`** returns true (matched embeddable video); otherwise static text rows with muted titles and no hover. Optional **`hideTrackPosition`** drops the position column (preview videos). **`reserveQueueColumn`** keeps an empty queue slot on non-playable rows so durations align with playable rows and preview videos. When **`onAddAllToQueue`** is set, a toolbar below the list (**`fmdReleaseTracklistAddAllButton`**) adds every playable track in tracklist order (disabled when all are already queued). Per-track **`artists`** / **`extraartists`** credits on Various Artists comps; visible **Add to queue** (list-plus icon) on playable rows; animated bars or pause icon on the dock’s active track |
| `ReleaseTracklistSkeleton.component.tsx` | Pulsing placeholder rows matching tracklist layout while **`useDiscogsReleaseQuery`** loads |
| `useReleaseModalPlayback.hook.ts` | Modal playback state; track select calls **`startPlayback`**; **`handleTrackQueue`** calls **`addToQueue`**; **`handleAddAllToQueue`** queues every playable album track not already in the queue |

**Similar in your collection:** **`getSimilarReleases`** scores other collection items with weighted tag overlap — **styles** count more than **genres** — plus small boosts for shared **label** (by Discogs **`labels[].id`**, with name fallback) and nearby **year** (±5), and a **same-artist penalty** (by **`artists[].id`**, with name/ANV fallback) so gig-list discovery favors different artists with a similar vibe. Compound tags also match on **tokens split from non-alphanumeric characters** (e.g. **`Italo-Disco`** overlaps **`Disco`**, **`Garage House`** overlaps **`House`**) when each token is at least three characters. When the source has styles, candidates must share at least one style tag (including compound segments); genre-only overlap is not enough. Results dedupe to **one row per Discogs release id** (`basic_information.id` via **`parseReleaseId`**) and **one per `master_id`** (highest score wins). Excludes the open instance, other collection copies of the same release id, and same **`master_id`** as the source. Untagged sources return no matches. Similar matches are computed only after the full collection has loaded; until then **`ReleaseSimilarSidebar`** shows **`PageLoader`** (**`Loading similar releases…`**, same pattern as crate drawer loading). Returns up to **`RELEASE_MODAL_SIMILAR_LIMIT`** (16) matches sorted by score then title. Covered by [`ReleaseModal.similar.spec.tsx`](../../src/components/ReleaseModal/ReleaseModal.similar.spec.tsx) (mobile inline placement + desktop aside; **`ReleaseModal.po`** exports **`mockUseMediaQuery`** for breakpoint tests).

## Feature example: ReleasePlayback

Persistent background playback is split across sibling component folders. **`ReleasePlaybackProvider`** ([`releasePlayback.context.tsx`](../../src/context/releasePlayback.context.tsx)) and **`GlobalPlaybackDock`** load through **[`AuthenticatedProvidersGate`](../../src/components/AuthenticatedProvidersGate.component.tsx)** → **[`AuthenticatedProviders`](../../src/components/AuthenticatedProviders.tsx)** (client-only chunk) so the dock and YouTube iframe survive client-side navigation without breaking static prerender. Playback-enabled pages register modal open handlers with **`useRegisterPlaybackReleaseClick`** ([`playbackReleaseClick.context.tsx`](../../src/context/playbackReleaseClick.context.tsx)).

| File | Role |
|------|------|
| [`GlobalPlaybackDock.component.tsx`](../../src/components/GlobalPlaybackDock/GlobalPlaybackDock.component.tsx) | Single persistent dock for all authenticated routes. Portals **[`PlaybackDockBar`](../../src/components/PlaybackDockBar/PlaybackDockBar.component.tsx)** to **`document.body`** so the YouTube iframe survives client-side navigation (e.g. Dashboard ↔ Releases) without remounting. Public crate pages use the same dock plus **[`PlaybackScrollSpacer`](../../src/components/PlaybackScrollSpacer/PlaybackScrollSpacer.component.tsx)** in document flow. |
| [`PlaybackDockBar.component.tsx`](../../src/components/PlaybackDockBar/PlaybackDockBar.component.tsx) | Shared mini-player chrome wrapper around **[`ReleaseMiniPlayer`](../../src/components/ReleaseMiniPlayer/ReleaseMiniPlayer.component.tsx)**. Returns **`null`** until **`ReleasePlaybackProvider`** is mounted (**`useHasReleasePlaybackProvider`**) so prerender stays safe before **`AuthenticatedProviders`** loads. Fixed to the viewport bottom via **`GlobalPlaybackDock`**. Optional **`inFlow`** → **`position: static`** (**`data-playback-dock-in-flow`**) for alternate layouts; collection shells use a footer spacer instead. |
| [`CollectionPlaybackPageShell.component.tsx`](../../src/components/PlaybackPageShell/CollectionPlaybackPageShell.component.tsx) | Authenticated collection-page wrapper around **[`PlaybackPageShell`](../../src/components/PlaybackPageShell/PlaybackPageShell.component.tsx)**: splits **[`StickyHeaderBar`](../../src/components/StickyHeaderBar/StickyHeaderBar.component.tsx)** into **`part="nav"`** (**`header`**) and **`part="filters"`** (**`subheader`**, omitted when **`hideFilters`**) — each **`part`** renders only its subtree — so modals portal over filter bars + scroll content. Always passes **`fillViewport`** to the inner shell. Props: **`currentPage`**, **`allReleasesLoaded`**, **`hideFilters`**, optional **`overlays`**, **`scrollElement`**, **`mainClassName`**. Used by **`/releases`**, **`/dashboard`**, **`/mosaic`**, **`/crates`**, **`/crates/[id]`**, **`/settings`**, **`/admin`**, and **[`AppPageLoading`](../../src/components/AppPageLoading/AppPageLoading.component.tsx)**. |
| [`PlaybackPageShell.component.tsx`](../../src/components/PlaybackPageShell/PlaybackPageShell.component.tsx) | Shared **`100dvh`** flex column: optional **`header`** (top nav, flex-shrink 0), optional **`subheader`** (e.g. desktop **`FiltersBar`** — lives inside **`[data-playback-page-workspace]`**), **`main`** (flex 1, scroll root), optional **`overlays`** in **`.workspaceOverlays`** (absolute inset on the workspace — **`BackToTop`**, mobile crate FAB, release modal roots), **`[data-playback-overlay-portal]`** on the workspace (covers **`subheader`** + **`main`** so release modals sit over filter bars), footer **`[data-playback-dock-spacer]`** (flex-shrink 0 — reserves space for the fixed **`GlobalPlaybackDock`** mini player). Wraps children in **[`PlaybackPageShellProvider`](../../src/components/PlaybackPageShell/PlaybackPageShell.context.tsx)** (scroll root, overlay portal element, ref-counted overlay lock, optional scroll-to-top handler registration). **`fillViewport`** pins the shell to **`100dvh`** (set by **`CollectionPlaybackPageShell`** on all authenticated collection routes). **[`AppDialog`](../../src/components/AppDialog/AppDialog.component.tsx)** portals into **`overlayPortalElement`** when set (absolute backdrop/popup within the workspace). External **`scrollElement`** ( **`/releases`** only) keeps the scroll root inside page content; the dock spacer stays in the shell column. Context exports **`usePlaybackPageScrollElement`**, **`usePlaybackPageScrollToTop`**, **`usePlaybackPageScrollToTopRegistration`**, **`usePlaybackPageOverlayPortal`**, **`usePlaybackPageScrollLock`**, and **`usePlaybackPageScrollLockCountRef`**. Consumers: **[`BackToTop`](../../src/components/BackToTop/BackToTop.component.tsx)** (**`usePlaybackPageScrollToTop`** — falls back to smooth **`scrollTo`** when no handler registered), virtualized **`/releases`** grids/tables (**`useVirtualizedReleaseGrid`**, **`ReleasesTable`** register **`scrollToIndex(0)`**), **[`AppDialog`](../../src/components/AppDialog/AppDialog.component.tsx)**, **[`BottomDrawer`](../../src/components/BottomDrawer/BottomDrawer.component.tsx)**. |
| [`BottomDrawer.component.tsx`](../../src/components/BottomDrawer/BottomDrawer.component.tsx) | Mobile bottom sheet (crate drawer, filters drawer, mobile menu): portaled to **`document.body`**, **`position: fixed`**, slide-up animation, default **`--z-10-bottom-drawer`**. **`data-testid="fmdBottomDrawer"`** is on **`drawerShell`** (slide-up / **`max-height`** / **`aboveMiniPlayer`** host), not the inner panel. Pass **`chrome`** for the shared drawer shell from [`bottom-drawer-chrome.module.css`](../../src/styles/modules/bottom-drawer-chrome.module.css) (**`drawertitle`** meta label, **`drawerheader`** microdot header, **`drawerpanel`** bordered shell); optional **`contentFlush`** for zero-padding scroll content. **`closeButtonPlacement`** defaults to **`header`** when **`chrome`** is set; the crate drawer passes **`floating`**, which wraps the panel and pill close in **`drawerShell`** (**`drawerShellWithFloatingClose`**). **`floatingShellClose`** is the shell’s first child, **`top: 0`** in reserved **`padding-top`** (**`--bottom-drawer-floating-close-offset`**) — not negative overflow above the sheet — so iOS can tap it below browser chrome; header controls like **New Crate** keep full width inside **`headerChrome`**. **`usePlaybackPageScrollLock(isOpen)`** while open unless **`hideOverlay`** or **`inline`**. **`max-height`** uses **`100dvh`** minus **`--bottom-drawer-top-inset`** (**`--sticky-header-height` + `env(safe-area-inset-top)`** — mobile drawers, not desktop filters bar) and **`--bottom-drawer-bottom-inset`** (**`--release-mini-player-stack-offset`** + gap when **`aboveMiniPlayer`**); floating close subtracts **`--bottom-drawer-floating-close-offset`** again so the reserved pill row stays on-screen. **`aboveMiniPlayer`** anchors the sheet above **`--release-mini-player-stack-offset`** but keeps overlay/drawer **`z-index`** **below** **`GlobalPlaybackDock`** so the slide-up animation does not cover the mini player or mobile video panel; nested menus still portal above via **`OverlayStack`**. Menu, filters, and crate drawers pass **`aboveMiniPlayer`** when the dock is visible. Filter **`Select`**, **`AutocompleteSelect`**, and **`FilterViewsMenu`** inside **`FiltersDrawer`** use **[`OverlayStack`](../../src/components/OverlayStack/OverlayStack.component.tsx)** with **`escapeStackingContext`** so dropdowns portal to **`document.body`** at **`calc(var(--z-10-bottom-drawer) + 1)`** above **`GlobalPlaybackDock`** — see **`FiltersDrawer.spec.tsx`**. Optional **`dataAttribute`** on the overlay, or on the drawer when **`hideOverlay`** / **`inline`**, for global layout hooks (e.g. **`data-filters-drawer-open`**, **`data-crate-drawer-open`**, **`data-mobile-menu-open`**, **`data-playback-queue-open`**). Scroll inside **`.content`** only. |
| `ReleaseMiniPlayer.component.tsx` | Bottom dock via **`miniPlayerRoot`** (fixed to the viewport through **`GlobalPlaybackDock`**). Compact cover, single-line artist · title marquee via **[`ReleaseMiniPlayerMarquee`](../../src/components/ReleaseMiniPlayer/ReleaseMiniPlayerMarquee.component.tsx)**, prev/play-pause/next, **`ReleaseCrateMenu`** crate control. Cover size from **`--release-mini-player-cover-size`** in [`playback-dock.module.css`](../../src/styles/modules/playback-dock.module.css). Visible while **`isMiniPlayerVisible`**. **Queue** button opens **[`PlaybackQueueDrawer`](../../src/components/PlaybackQueueDrawer/PlaybackQueueDrawer.component.tsx)** with a count badge. Video panel above the bar on mobile; on desktop, a floating panel defaults above the release metadata (left of the transport controls). Collapses the mobile video panel when the filters or crate drawer opens (**`useFiltersDrawerOpen`**, **`useCrateDrawerOpen`**). Other drawers use **[`BottomDrawer`](../../src/components/BottomDrawer/BottomDrawer.component.tsx)** **`aboveMiniPlayer`** above the dock via inherited CSS vars from **`html:has([data-global-playback-dock])`**. Cover/title calls the registered **`onReleaseClick`** handler when the active page provides one. |
| `PlaybackQueueDrawer.component.tsx` | Bottom drawer listing **`queue`** from **`ReleasePlaybackProvider`**: drag handle reorder (**`reorderQueue`** via `@dnd-kit`), release cover, play at index, remove row, **Clear queue** (**`clearQueue`** — keeps the active track playing). Uses **[`BottomDrawer`](../../src/components/BottomDrawer/BottomDrawer.component.tsx)** with **`closeButtonPlacement="header"`**, **`behindMiniPlayer`**, **`hideOverlay`**, and **`inline`** (rendered inside **[`ReleaseMiniPlayer`](../../src/components/ReleaseMiniPlayer/ReleaseMiniPlayer.component.tsx)** **`miniPlayerRoot`**, not portaled — panel slides up from **`bottom: 0`** under the bar; **`padding-bottom`** clears the bar; bar stays on top via sibling **`z-index`**), plus drawer **`data-playback-queue-open`**. On desktop (≥768px) the panel is right-aligned, max **`31.25rem`** (~500px) wide. |
| `PersistentYoutubeIframe.component.tsx` | Single persistent YouTube embed (hidden or visible) via **`buildYoutubeEmbedUrl`** (`controls=1`, `enablejsapi=1`). **Hidden** audio mode keeps a **640×360** iframe off-screen (not 1×1) so YouTube initializes player chrome correctly. **Visible** track switches update **`iframe.src`** (full embed params); hidden switches use postMessage **`loadVideoById`**. Opening the video panel (**hidden → visible**) resumes playback without reloading **`src`** when the embed already has the current video (including after hidden track switches). Cross-release switches remount the iframe (**`key={release.instance_id}`**). The dock binds **`playbackVideoId`**. Modal track play passes **`youtubeVideoId`**. **`playVideo`** / **`pauseVideo`** / **`getPlayerState`** use postMessage via [`postYoutubePlayerCommand.ts`](../../src/utils/postYoutubePlayerCommand.ts). **`ReleasePlaybackProvider`** ignores embed **pause** events while the document is hidden (background-tab auto-pause) so queue **ended** handling still runs; on **visibilitychange** back to visible it requests player state to catch up missed **ended** events. Registers with the provider via a stable ref callback; **`load`** calls **`notifyPlaybackIframeLoaded`** for queue auto-advance and dock play/pause sync |

Closing **`ReleaseModal`** does not stop playback. **Play in background** or a matched track row click calls **`startPlayback`**, replaces the queue with playable tracks from that album starting at the clicked row, and overwrites whatever is in the dock. Unmatched embeddable videos (deduped by YouTube id) render in **`ReleasePlaybackPreview`** as track-style rows without a position column; row click calls **`startReleasePreview`**, and **Add to queue** calls **`addPreviewToQueue`**. **`findVideoForTrack`** ([`releasePlayback.ts`](../../src/utils/releasePlayback.ts)) matches on normalized substring overlap, an alphanumeric form (letters and numerals only — punctuation, brackets, and spaces stripped) with subsequence fallback for reordered text/digits (e.g. track **`161白A`** matches video **`YYY - 白161A [YYY161]`**), and **track position** when the video title includes a side suffix (e.g. duplicate track titles **`金344`** on A/B with video **`YYY - 金344 B`** matching position **B** only), plus shared title tokens after stripping artist prefixes from video titles. When title matching leaves equal counts of **generic** track titles (**`Untitled`**, **`Untitled01`**, **`Unknown`**, **`Track N`**, …) and leftover videos, **`buildReleasePlaybackMatchIndex`** can pair them 1:1 by tracklist order vs video **`Track N`** numbering (or video order) when every pair’s durations agree within **30s**. **`buildReleasePlaybackMatchIndex`** runs that matrix once per release (embeddable videos, per-track matches, preview leftovers); **`useReleaseModalPlayback`** and **`ReleasePlaybackProvider`** both memoize it so row playability, preview lists, and dock **`activeVideo`** resolution stay aligned. **`getReleasePreviewVideos`** lists embeddable videos not matched to any track. **Add to queue** (list-plus icon on a track row) calls **`addToQueue`** (deduped by **`instanceId` + `trackPosition`**). Prev/next walk the full **`queue`** (cross-release); when a video ends, the provider auto-advances via [`youtubeIframeEvents.ts`](../../src/utils/youtubeIframeEvents.ts). Queue helpers live in [`src/utils/playbackQueue.ts`](../../src/utils/playbackQueue.ts).

Crate, notes, and card filter pill clicks do **not** open the modal. Discogs links live on title, artist, label text, and the cover overlay **View on Discogs** button. On **`/releases`**, **cover art** or the overlay **Release details** button opens the in-app modal; **title** still opens Discogs.

## Feature example: ReleaseNotes

[`src/components/ReleaseNotes/`](../../src/components/ReleaseNotes/) follows the standard folder layout plus a colocated state hook:

| File | Role |
|------|------|
| `ReleaseNotes.component.tsx` | **`variant="modal"`** (release modal body): inline **`ReleaseNotesModalEditor`** when editable; read-only stacked **`field-label`** rows. Also **`inline`**, **`table`**, **`crate`** scratchpad, **`displayOnly`** (tests) |
| `ReleaseNotesCardAction.component.tsx` | Sticky-note icon — **`variant="card"`** (image overlay + tooltip) or **`variant="mobile"`** (stacked action column); primary dot badge when notes exist |
| `ReleaseNotesEditor.context.tsx` | Per-card provider so the icon and body share one editor/dialog |
| `ReleaseNotesEditorDialog.component.tsx` | Mounts **`NoteEditDialog`** from provider context when **`isDialogOpen`** (card/mobile overlay note icon only—not the release modal notes section) |
| `useReleaseNotesEditor.hook.ts` | Dialog state, save handler; patches **`DiscogsCollectionQueryKeys`** + IndexedDB (same collection-cache pattern as rating editor) |
| `NoteEditDialog.component.tsx` | Release notes editor via **`ScrollModal`** + **`ModalToolbar`** (`data-testid="fmdNoteEditDialog"`); shared **`ReleaseNotesFormFields`** (textarea note fields, then Media/Sleeve Condition **`Select`** dropdowns) |
| `ReleaseNotesFormFields.component.tsx` | Shared note textarea + condition **`Select`** row; all labels compose **`field-label.module.css`** |
| `ReleaseNotesModalEditor.component.tsx` | Inline release-modal notes editor (debounced textarea save, immediate condition saves). Save progress uses **`showReleaseNotesSavingToast`** / **`showReleaseNotesSavedToast`** ([`src/utils/toast.ts`](../../src/utils/toast.ts), shared toast id; success auto-dismisses after 2s); inline footer shows errors only |
| `ReleaseNotesModalSkeleton.component.tsx` | Notes textarea + condition **`Select`** placeholders while collection fields load |
| `NoteEditDialog.spec.tsx` / `ReleaseNotes.spec.tsx` | Dialog + modal inline editor tests |
| `ReleaseNotes.po.tsx` | Page object (`data-testid="fmdReleaseNotes"`) |

Wrap **`ReleaseCard`** and **`MobileReleaseCard`** with **`ReleaseNotesEditorProvider`**. **`ReleaseNotesCardAction`** (grid overlay / mobile action column) reads **`useReleaseNotesEditorContext()`** to open **`NoteEditDialog`**. Neither card variant renders inline note body copy—do not call **`useReleaseNotesEditor`** twice on the same card.

List/table rows use **`ReleaseNotes`** without the provider; only the **`inline`** subcomponent calls **`useReleaseNotesEditor`** directly.

**`variant="crate"`** (crate detail rows): empty state uses a dashed **Add notes** affordance; filled notes are plain muted body copy with a small underlined **Edit notes** link below.

## Dashboard release rows

Dashboard sections that list releases (On this day, most crated, milestones, duplicates modal list, etc.) share one layout — do not invent per-section cover/title/meta markup or card chrome:

| Piece | Location |
|-------|----------|
| Card shell | [`dashboard-card.module.css`](../../src/styles/modules/dashboard-card.module.css) — **`.releaseRow`** for bordered list rows (composes **`.card`**) |
| Row content | [`DashboardReleaseItem.component.tsx`](../../src/components/Dashboard/DashboardReleaseItem.component.tsx) + [`DashboardReleaseItem.module.css`](../../src/components/Dashboard/DashboardReleaseItem.module.css) |

Section modules may add layout wrappers around **`.releaseRow`**, but the inner release presentation stays in **`DashboardReleaseItem`**. Optional trailing slots (e.g. **[`OnThisDay`](../../src/components/Dashboard/OnThisDay.component.tsx)** year added, **[`MostCrated`](../../src/components/Dashboard/MostCrated.component.tsx)** crate count) use **`children`**. Default title/meta lines single-line ellipsis; pass **`wrapText`** on **`DashboardReleaseItem`** when a section needs wrapped copy (On this day). **[`OnThisDay.module.css`](../../src/components/Dashboard/OnThisDay.module.css)** caps list width (~**`36rem`**) so rows do not span the full dashboard. See [patterns.md → Dashboard analytics → Card chrome](patterns.md#dashboard-analytics).

## Client page shells

| Shell | Route | Layout |
|-------|-------|--------|
| [`Login`](../../src/components/Login/Login.component.tsx) + [`PublicAuthLayout`](../../src/components/PublicAuthLayout/PublicAuthLayout.component.tsx) | `/`, about, legal, public crate | Server `PageFooter`, optional authenticated header |
| [`ReleasesClient`](../../src/components/ReleasesClient/ReleasesClient.component.tsx) | `/releases` | [`Page`](../../src/components/Page/Page.component.tsx) + [`CollectionPlaybackPageShell`](../../src/components/PlaybackPageShell/CollectionPlaybackPageShell.component.tsx) + [`CrateDrawer`](../../src/components/CrateDrawer/CrateDrawer.component.tsx) |
| [`CratesClient`](../../src/components/Crates/CratesClient.component.tsx) / [`CrateHubCard`](../../src/components/Crates/CrateHubCard.component.tsx) / [`CrateDetailClient`](../../src/components/Crates/CrateDetailClient.component.tsx) | `/crates`, `/crates/[id]` | [`Page`](../../src/components/Page/Page.component.tsx) + [`CollectionPlaybackPageShell`](../../src/components/PlaybackPageShell/CollectionPlaybackPageShell.component.tsx); hub card grid + owner crate detail workspace |
| [`DashboardClient`](../../src/components/Dashboard/DashboardClient.component.tsx) | `/dashboard` | [`Page`](../../src/components/Page/Page.component.tsx) + [`CollectionPlaybackPageShell`](../../src/components/PlaybackPageShell/CollectionPlaybackPageShell.component.tsx) (**`hideFilters`**) + narrative analytics; release modal in **`overlays`** |
| [`SettingsClient`](../../src/components/Settings/SettingsClient.component.tsx) | `/settings` | [`Page`](../../src/components/Page/Page.component.tsx) + [`CollectionPlaybackPageShell`](../../src/components/PlaybackPageShell/CollectionPlaybackPageShell.component.tsx) (**`hideFilters`**) + sidebar section panels |
| [`MosaicClient`](../../src/components/MosaicClient/MosaicClient.component.tsx) | `/mosaic` | [`Page`](../../src/components/Page/Page.component.tsx) + [`CollectionPlaybackPageShell`](../../src/components/PlaybackPageShell/CollectionPlaybackPageShell.component.tsx) (filters in **`subheader`**) via [`MosaicClientWrapper`](../../src/components/MosaicClient/MosaicClientWrapper.component.tsx) |
| [`AdminDashboardClient`](../../src/components/AdminDashboard/AdminDashboardClient.component.tsx) | `/admin` | [`Page`](../../src/components/Page/Page.component.tsx) + [`CollectionPlaybackPageShell`](../../src/components/PlaybackPageShell/CollectionPlaybackPageShell.component.tsx) (**`hideFilters`**) + admin stats |
| [`AnalyticsPageViewTracker`](../../src/components/GoogleTagManagerLoader/AnalyticsPageViewTracker.component.tsx) | global (`Providers`) | Consent-gated **`pageView`** events on App Router navigations |

Login marketing assets live under [`public/images/`](../../public/images/) and theme-aware demo components under [`Login/`](../../src/components/Login/).
